services:
  backend:
    container_name: backend
    build: .
    env_file:
      - .env
    ports: 
      - "5000:5000"
    depends_on:
      - db

  db:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example